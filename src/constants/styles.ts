const TRANSITION = "all 0.15s cubic-bezier(0, 0, 1, 1)";

const BORDER_RADIUS = "4px";
const BORDER_COLOR = "#d5d7da";

const BACKGROUND = "#f7f7f8";
const COLOR = "#101928";
const FONT_SIZE = "14px";

const DISABLE_COLOR = "#dee3e7";

export const STYLES = {
  TRANSITION,

  BORDER_RADIUS,
  BORDER_COLOR,

  BACKGROUND,
  DISABLE_COLOR,
  COLOR,
  
  FONT_SIZE,
};
